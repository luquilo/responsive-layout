// membuat function dengan baik

const coba = ({ nama, nim, kelas }) => {
  console.log(`halo, nama saya ${nama}, dan nim saya ${nim}, saya dari kelas ${kelas}`);
};

coba({ nim: 2113010276, kelas: '4g', nama: 'luqman' });
